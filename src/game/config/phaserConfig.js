import MainScene from "../MainScene";

const phaserConfig = {
  type: Phaser.AUTO,
  width: 1600,
  height: 1024,
  backgroundColor: "#202428",
  pixelArt: true,
  scene: [MainScene],
};

export default phaserConfig;
