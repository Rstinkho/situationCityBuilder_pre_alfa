import MainScene from "../MainScene";

const phaserConfig = {
  type: Phaser.AUTO,
  width: 1024,
  height: 640,
  backgroundColor: "#202428",
  pixelArt: true,
  scene: [MainScene],
};

export default phaserConfig;
